function getAllItemId(){
    var divElement = document.getElementById('cart-table');
    var inputElements = divElement.getElementsByTagName('input');
    for (int i = 0; i<inputElements.length; i++){
        if (inputElements[i].type != '')
    }
}